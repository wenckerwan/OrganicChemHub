# OrganicChemHub 更新日志

> 当前基准版本：v3.0
> 当前开发版本：v4.0
> 更新方向：图片结构式、内容质量、后台运营、学习体验。不再规划文本结构式生成、结构编辑器或子结构检索路线。

---

## 后续规划

### v3.0 — 公开稳定版（已完成，2026-08-20）

- 占位图工作流：为缺图反应一键生成中性占位图（方程图 + 缩略图），让内容通过发布校验并上线，待替换真图。
- 图片维护工具增强：按缺失类型筛选、全量分页展示、批量生成占位图（写入 OpLog + ContentBatch）。
- 内容就绪报告：`content_readiness_report` 管理命令输出各库就绪度统计与逐条缺失清单 CSV。
- 发布流程闭环：`publish_ready_content` 发布后写操作日志与内容批次；仪表盘增加占位图待替换计数。
- 零模型变更、零迁移。

**当前实现范围（2026-08-20）：** `reactions/services/placeholder.py`（equation_svg/thumbnail_svg/placeholder_for）；图片维护页全量分页 + missing 筛选 + 批量生成占位图；`reactions/services/readiness.py`（summary/export_rows）+ `content_readiness_report` 命令；`publish_ready_content` 写 OpLog + ContentBatch；仪表盘 `placeholders_pending` 计数；V30 专项测试 9 个。

### v4.0 — 待定

- 后续版本规划待 v3.0 验收后补充。

---

## 版本历史

### v2.7 合成路线扩充（2026-08-20，已完成）

- 模型迁移 `0021_routestep_is_key_step_and_more`：`RouteStep.is_key_step` 关键步骤、`SyntheticRoute.related_functional_groups` 官能团关联、`get_key_step_count()`。
- 步骤序号连续性校验（`RouteStep.clean`，从 1 起连续无缺号）。
- 后台增强：官能团选择/筛选、关键步骤列、缺步骤图提醒列、内联图片缩略图预览。
- 前台：路线列表官能团筛选下拉（与难度/搜索/排序叠加）、详情页官能团徽标回跳、关键步骤时间线金色高亮。
- V27 专项测试 12 个，全量 132/132 通过。

### v2.6 学习进度与复习系统（2026-08-20，已完成）

- 新增 `reactions/services/progress.py` 进度服务层：专题进度统计、状态映射、专题聚合、复习清单、最近学习。
- 专题详情页"我的专题进度"进度条，反应卡片内联状态切换，路线状态徽标。
- 个人中心新增专题进度聚合、复习清单、最近学习记录三个区块。
- 零模型变更，全部复用现有 `StudyProgress`；V26 专项测试 21 个，全量 120/120 通过。

### v2.5 考研专题学习（2026-08-20，已完成）

- 新增 `StudyTopic` 考研专题模型，支持关联人名反应、常见有机反应和合成路线。
- 新增 `ReactionComparison` 易混反应对比模型，反应 A/B 使用通用内容关联，强制两个反应不同。
- 新增专题和对比前台列表/详情页，支持搜索、分页、访问统计和只展示已发布内容。
- 首页新增考研专题和易混反应对比入口。
- 个人中心新增待学习、待复习和已学内容复习区块。
- 用户互动模型（`Favorite`/`StudyNote`/`StudyProgress`）改用通用内容关联，同时支持新老反应库；详情页收藏、笔记和学习进度控件正式启用。
- 专题详情页补充易混反应对比入口。

### v2.4 内容质量增强（2026-08-03）

- `ReactionImage` 新增图片审核状态：待审核、已通过、需重画。
- 后台支持按审核状态筛选图片，并批量标记已通过或需重画。
- 内容质量仪表盘增加图片审核数量和高访问但未完整内容列表。
- 人名反应和常见反应管理列表增加总访问量、今日访问量。

### v2.3 访问统计（2026-08-03）

- 新增 `VisitCounter` 访问统计模型，支持网站、反应详情页和合成路线详情页计数。
- 新增前台访问统计卡片：首页显示网站总访问量/今日访问量，详情页显示本页总访问量/今日访问量。
- 新增 `SiteVisitCounterMiddleware`，自动统计前台 GET 访问，排除后台、静态文件和媒体文件。
- 后台新增“访问统计”只读列表，防止人工误改统计值。

### v2.2 反应多图上传（2026-08-03）

- 新增 `ReactionImage` 反应附图模型，支持人名反应和常见有机反应共用。
- 方程式图、机理图、反应条件附图、机理文字附图、考点附图均支持多张上传，每个区域上限 10 张。
- 条件、机理文字和考点附图为可选项，不影响发布。
- 多张方程式图可满足发布完整度检查，仪表盘不再误判为缺方程式图。
- 详情页新增按区域展示的附图画廊。

### v2.1 内容发布完善（2026-08-03）

- 首页增加“前台内容状态”，展示已发布人名反应、常见有机反应、合成路线和待发布完整内容数量。
- 后台内容质量仪表盘增加“可发布草稿”统计。
- 新增 `publish_ready_content` 管理命令，完整草稿可批量发布，缺图或缺关键字段内容自动跳过。
- 清理旧结构式生成运行依赖，前台结构式展示继续以后台上传图片为准。

### v2.0 后台内容管理正式版（2026-08-03）

- 人名反应和常见有机反应拆成两套内容库。
- Django Admin 作为标准内容管理层，专用页面负责仪表盘、CSV 导入、图片维护、消息群发和资料上传登记。
- 前台保留现有 UI 风格，同时区分人名反应库和常见有机反应库。
- 反应方程式图和缩略图为发布质量检查项，机理图可选。

### v1.6 前台反应库 UI 与导航分流（2026-08-03）

- 人名反应库使用蓝色主题。
- 常见有机反应库使用青绿色主题。
- 修复旧导航项导致常见反应进入人名反应库的问题。
- 首页“进入后台维护”按钮仅管理员可见。

### v1.5 反馈升级与站内消息（2026-07-29）

- 反馈系统支持类型分类、状态流转、管理员公开回复和内部备注。
- 新增站内消息中心、未读红点、全部已读。
- 公告发布、反馈回复和状态变更可触发站内消息。

### v1.4 用户体系与个性化（2026-07-29）

- 用户注册、登录、退出。
- 收藏反应和路线。
- 私人笔记和学习进度。
- 个人中心展示收藏、笔记、反馈和进度统计。

### v1.3 公告弹窗与意见反馈（2026-07-29）

- 公告栏、重要公告弹窗、置顶和显示时间段。
- 前台意见反馈表单。
- 后台可查看和处理反馈。

### v1.2 后台工具增强（2026-07-29）

- CSV 导出。
- 内容质量仪表盘。
- 导入字段补充。
- Git 同步流程文档。
